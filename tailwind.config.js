const { colors } = require('tailwindcss/defaultTheme')

module.exports = {
  theme: {
    extend: { colors },
    colors,
    container: {
      center: true,
    },
  },
  variants: {},
  plugins: []
}
